const startRecordingButton = document.getElementById('start-recording');

startRecordingButton.addEventListener('click', () => {
  alert('Recording will start soon!'); 
  // TODO add screen recording logic here later
});